@{
    ViewData["Title"] = "Payment Page";
} 

<section class="payment-container">
    <h1>Payment Page</h1>
    <div class="payment-options">
        <div class="payment-option">
            <input type="radio" name="payment-method" id="debit-credit" value="debit-credit">
            <label for="debit-credit">Debit/Credit Card</label>
        </div>
        <div class="payment-option">
            <input type="radio" name="payment-method" id="cod" value="cod">
            <label for="cod">Cash on Delivery</label>
        </div>
    </div>

    <!-- Forms for each payment method -->
    <div id="debit-credit-form" class="payment-form" style="display: none;">
        <div class="form-group">
            <label for="card-number">Card Number</label>
            <!-- Card number validation: only digits, max length 16 -->
            <input type="text"
                   id="card-number"
                   placeholder="Enter Card Number"
                   maxlength="16"
                   pattern="^[0-9]{16}$"
                   title="Please enter exactly 16 digits"
                   required>
            <span id="card-error" class="error-message"></span>
        </div>
        <div class="form-group">
            <label for="cvv">CVV</label>
            <!-- CVV validation: only digits, max length 3 -->
            <input type="text"
                   id="cvv"
                   placeholder="CVV"
                   maxlength="3"
                   pattern="^[0-9]{3}$"
                   title="Please enter exactly 3 digits"
                   required>
            <span id="cvv-error" class="error-message"></span>
        </div>
    </div>
    <div id="cod-form" class="payment-form" style="display: none;">
        <p>Cash on Delivery selected. Please confirm your order to proceed.</p>
    </div>

    <!-- Form Actions -->
    <div class="form-actions">
        <button class="submit-button" onclick="validateForm()">Submit</button>
        <button class="cancel-button" onclick="cancelPayment()">Cancel</button>
    </div>

    <!-- Error Message -->
    <div id="error-message" class="error-message"></div>
</section>
